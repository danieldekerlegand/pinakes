# ML link-prediction baselines (Phase 2, US-003)

TransE / ComplEx / RotatE knowledge-graph embedding baselines trained on the
committed triples splits (US-002). These establish the **metric floor** all
future corpus growth and neurosymbolic work is judged against — so every
number below is pinned to an exact corpus version, split manifest, and seed.

> Regenerated by `uv run pinakes-train-baselines` (see *Reproduce*).
> This file is committed; the trained embeddings and MLflow runs are not (DVC-tracked / git-ignored respectively).

## Corpus & split version

| Artifact | Hash |
| --- | --- |
| Canonical export (`export/culturescrape`, DVC md5) | `e418c976755c57876e0be1438c6295b7.dir` |
| Split manifest (`ml/manifests/triples-split-manifest.json`, sha256) | `9074b365176c17f5fc83e1339b55ca993576b753b811abb935946b888aa1672a` |
| Triples content (`triplesSha256`) | `c799a6f1767e0e876c7b3f18ed7ec4031fa2af0d3c68420e0101aa07059ff2bb` |

- Triples: **2267** (train 1815 / valid 227 / test 225), **2057** entities, **8** relations.
- Seed: **20260712** · device: **cpu** · embedding_dim: **64** · epochs: **100**.

## Results

Filtered link-prediction metrics (both sides, realistic rank) on the **test**
split, evaluated against the full entity vocabulary.

| Model | MRR | Hits@1 | Hits@3 | Hits@10 |
| --- | --- | --- | --- | --- |
| TransE | 0.0071 | 0.0000 | 0.0067 | 0.0111 |
| ComplEx | 0.0019 | 0.0000 | 0.0000 | 0.0000 |
| RotatE | 0.0214 | 0.0089 | 0.0244 | 0.0400 |

> The corpus is small and sparse (≈one triple per entity) and the splits are
> grouped by unordered entity pair to prevent inverse leakage, so many test
> entities are unseen in training — these are a deliberately conservative
> floor, not a tuned result. Corpus growth should move them up.

## Logical consistency (US-005)

Each baseline's top-1 link predictions (both directions, per test
triple) are checked against the symbolic rules — **descent acyclicity**,
canonical-schema **from/to type** constraints (`shared/canonical-schema.json`),
and relation **antisymmetry** — by `pinakes_ml.consistency`. The violation
counts are ratcheted in CI against `ml/manifests/consistency-baseline.json`
(a monotone gate, mirroring `docs/convergence-qa-baseline.json`); a deliberate
rise is re-baselined with `uv run pinakes-check-consistency --write-baseline`.

| Model | Predictions | Descent cycles | Schema type breaches | Asymmetry violations |
| --- | --- | --- | --- | --- |
| TransE | 379 | 295 | 4 | 332 |
| ComplEx | 401 | 2 | 76 | 2 |
| RotatE | 403 | 0 | 43 | 0 |

> Predictions are the model's single best (top-1) head- and tail-completions of
> each test query (`ml/predictions/<model>.tsv`, committed). A deliberately
> conservative floor: with a near-random baseline most predictions are schema-
> plausible high-degree nodes, so violations start low — the ratchet guarantees
> future models (and corpus growth) never make the predictions *less* logically
> consistent without review.

## Trained embeddings

Each model's entity-embedding matrix is exported to
`ml/data/embeddings/<model>/entity_embeddings.npy` (row-aligned to
`ml/data/triples/entities.tsv` index order) with a `metadata.json` sidecar,
and DVC-tracked under `ml/data` for reuse (GraphRAG, the Scallop pilot).

## Reproduce

```bash
cd ml
uv run --project . dvc pull   # fetch DVC-tracked splits (from repo root)
uv run pinakes-train-baselines   # train all 3, log MLflow, rewrite doc
# re-pin embeddings: cd .. && uv run --project ml dvc add ml/data && dvc push
```

Metrics are logged to the `pinakes-baselines` MLflow experiment (local file backend; `uv run mlflow ui`).

<!-- KGQA-EVAL:START (generated by pinakes-eval-kgqa) -->

## KGQA accuracy (tier 3, US-004)

The third eval tier scores whether a system **answers the held-out multi-hop
reasoning questions correctly** (`ml/data/kgqa/eval.jsonl`, the registered
held-out split from US-003) and whether its answer is **grounded in the
evidence it retrieved**. Deterministic + offline (network-free), so the numbers
below are a byte-reproducible floor committed to `ml/manifests/kgqa-eval-baseline.json`.

- Held-out records: **394** (path 226 / derivation 168); retrieval depth **2**.

| System | Exact | Normalized | Grounding | Answered |
| --- | --- | --- | --- | --- |
| graph-retrieval | 98.5% | 98.5% | 100.0% | 98.5% |
| no-retrieval | 1.5% | 1.5% | 0.0% | 0.0% |

> `graph-retrieval` is the GraphRAG floor — it expands a depth-bounded
> neighbourhood around the subject and answers by walking the reasoning chain
> over the retrieved edges, so a chain deeper than the retrieval depth is
> answered wrong (an honest depth-limited floor, not an oracle). `no-retrieval`
> is the control: with no retrieved subgraph it can only restate the subject,
> so the gap between the two rows is the value the retrieved evidence adds.
> The *live* variant answers the same retrieved subgraphs through the existing
> Gemini proxy (`/api/graph/retrieve` + the LLM) and is measured locally — see
> `docs/kgqa-dataset.md`; it is never required for CI.

### Logical consistency over KGQA evidence (tier 2)

The structured evidence each system grounds its answers in is itself a set of
predicted edges, so the tier-2 checks (`pinakes_ml.consistency`) run over
it — descent acyclicity, canonical-schema `from`/`to` type constraints, and
relation antisymmetry. The retrieved evidence is drawn from real corpus edges,
so any breach here is a genuine corpus/schema observation (e.g. `DESCENDS_FROM`
edges among node types the schema's `from`/`to` sets do not yet declare), not a
model error — descent cycles and antisymmetry violations should stay at zero.

| System | Evidence edges | Descent cycles | Schema type breaches | Asymmetry violations |
| --- | --- | --- | --- | --- |
| graph-retrieval | 371 | 0 | 20 | 0 |
| no-retrieval | 0 | 0 | 0 | 0 |

## Evaluation tiers

The eval harness judges the corpus from three angles — cross-linked here so
a reader sees the whole picture:

1. **Statistical (tier 1)** — link-prediction MRR / Hits@k (*Results* above).
2. **Symbolic (tier 2)** — logical-consistency violation counts (*Logical
   consistency* above, and over KGQA evidence in this section).
3. **Question answering (tier 3)** — held-out KGQA accuracy + evidence
   grounding (this section).

Reproduce tier 3: `cd ml && uv run pinakes-eval-kgqa` (needs the
DVC-tracked export + eval split; `uv run --project ml dvc pull` first).

<!-- KGQA-EVAL:END -->

<!-- SCALLOP-PILOT:START (generated by pinakes-train-scallop) -->

## Rule-guided link prediction (Phase 5, US-003)

The neurosymbolic pilot: a neural edge predicate over the **transe** embeddings, with the corpus's *ancestor* transitive-closure rule propagating its soft edge probabilities under Scallop's `minmaxprob` provenance (`Pr[ancestor] = max-path of min-edge-prob`), trained on the US-002 queries and evaluated on the same held-out `test` split as the baselines. The headline verdict is the **ablation** (rule on vs the identical predicate with the rule off, one harness); the PyKEEN row is the committed floor, measured by its own evaluator.

> **Verdict:** rule guidance **neutral** — no material MRR change vs the direct-only ablation.

| System | MRR | Hits@1 | Hits@3 | Hits@10 |
| --- | --- | --- | --- | --- |
| PyKEEN transe (baseline floor) | 0.0071 | 0.0000 | 0.0067 | 0.0111 |
| Neural predicate, rule OFF (ablation) | 0.0062 | 0.0000 | 0.0029 | 0.0087 |
| Rule-guided (neural + ancestor rule) | 0.0060 | 0.0000 | 0.0000 | 0.0087 |

Filtered ranks over 344 head/tail queries (seed 20260713, 40 epochs, 3-hop min-max propagation, CPU). The neural-vs-PyKEEN gap reflects a different scorer *and* a different evaluator; the rule's own effect is the ablation rows.

### Logical consistency of the pilot's predictions (tier 2)

Each system's top-1 completions of the target-relation test queries run through the same `pinakes_ml.consistency` checks as the baselines — rule-off vs rule-on over the identical prediction set, so the rule's effect on logical consistency is isolated too.

| System | Descent cycles | Schema type breaches | Asymmetry violations |
| --- | --- | --- | --- |
| Neural predicate, rule OFF | 83 | 10 | 85 |
| Rule-guided | 295 | 5 | 297 |

> Reproduce: `cd ml && uv run pinakes-train-scallop` (needs the DVC-tracked embeddings + splits + queries; `uv run --project ml dvc pull` first). The engine-free min-max reference runs anywhere; the equivalent scallopy `minmaxprob` program (`build_scl_program`) is local-only (macOS/arm64 wheel). Full runbook: `docs/scallop-pilot.md` §US-003.

<!-- SCALLOP-PILOT:END -->

<!-- RULE-ADHERENCE:START (generated by pinakes-eval-rule-adherence) -->

## Prolog rule adherence (tier 4, insimul-bridge US-004)

The fourth eval tier scores **generated Prolog rules against the world they
were authored for** — a port of Insimul's VESPACE reachability analyser and
validator stack (metric definitions cited in [`docs/rule-adherence-tier.md`](rule-adherence-tier.md);
no Insimul code is imported). Deterministic + offline, so the numbers below
are a byte-reproducible floor committed to `ml/manifests/rule-adherence-baseline.json`.

- World `w-vespace-fixture` (`insimul-grounding-v1`): **11** intrinsic + **7** action-producible predicates over **5** actions; **7** entity atoms.
- Candidate rules: **8** (7 parsed).

| Check | Rate | Rules |
| --- | --- | --- |
| Parse | 87.5% | 7/8 |
| Structural validity | 85.7% | 6/7 |
| Schema validity | 57.1% | 4/7 |
| Referential integrity | 85.7% | 6/7 |

| Reachability metric | Mean | Median | Min | Max |
| --- | --- | --- | --- | --- |
| Charitable | 0.690 | 1.000 | 0.000 | 1.000 |
| Strict | 0.571 | 1.000 | 0.000 | 1.000 |
| Fireability index | 0.457 | 0.449 | 0.000 | 0.741 |

> 3 of 7 parsed rules carry at least one **dead condition** (a predicate no action produces and character creation never sets); 3 have a fully dead action slice. *Charitable* reachability assumes any intrinsic trait could have been authored onto some character; *strict* isolates the action-driven slice. The fireability index discounts charitable reachability by condition count (λ_intrinsic = 0.05, λ_action = 0.25), so a long rule that is nominally reachable still scores low.

| Dead condition key | Rules affected |
| --- | --- |
| `effect/2` | 1 |
| `esteems/3` | 1 |
| `married/2` | 1 |
| `trait_female/1` | 1 |
| `trusts/3` | 1 |

Reproduce: `cd ml && uv run pinakes-eval-rule-adherence` (fixture-driven — no DVC corpus needed); `--check` is the retraining-free ratchet gate.

<!-- RULE-ADHERENCE:END -->

<!-- SLM-PILOT:START (generated by pinakes-train-slm) -->

## SLM pilot — rule-authoring baseline (Phase D, slm-pilot US-003)

Base model `Qwen/Qwen2.5-3B-Instruct`, QLoRA on the DVC-pinned rule-SFT corpus, scored by the **tier-4 adherence harness** on the frozen eval set (`docs/slm-pilot-protocol.md`). 3 repeat(s) at one pinned seed; a cell shows the mean and, where the repeats disagreed, the observed range.

- eval set `sha256 e01cd8175823…`, frozen: `True` — 2 prompts, held out w-laterre
- trained on 3 records, DVC tree `f20307afdace3832ac9a415db5f57699.dir`
- device `mps`, wall clock 191.1 s, cost $0.00 (local Apple-Silicon MPS run — no GPU rental)

> ⚠️ **`dataFloor.verdict == "insufficient-data"`.** The numbers below describe a *pipeline that completes the loop*, not an approach that validated. Per protocol §5 no verdict about the method may be drawn from them; US-006 reads this field rather than re-deriving it.

| Comparison point | parseRate | schemaValidity | referentialIntegrity | fullyValid | reachabilityCharitable | evalLoss |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `deterministic-translator-floor` | — | — | — | — | — | — |
| `untuned-qwen2.5-3b-instruct` | 1.000 | 0.500 | 1.000 | 0.500 | 0.500 | 3.13 |
| `finetuned-qwen2.5-3b-instruct` | 0.500 | 1.000 | 1.000 | 1.000 | 0.000 | 2.46 |
| `finetuned-qwen2.5-3b-instruct-ungrounded` | 1.000 | 0.000 | 1.000 | 0.000 | 0.000 | 2.44 |
| `grounded-gemini` | — | — | — | — | — | — |

Not measured, and why — a frozen comparison point is never dropped, only explained:

- `deterministic-translator-floor` — Insimul's VESPACE deterministic translator is not present in the checkout at ~/Development/workspace/insimul-platform (the repo was restructured); frozen as not-measured by the protocol, §4.
- `grounded-gemini` — not requested — rerun with `pinakes-train-slm --gemini` and a GEMINI_API_KEY to fill this row.

### Ablation — does tuning internalise the vocabulary?

Fine-tuned weights, grounded prompt **minus** the same weights on the vocabulary-stripped prompt. Not part of the success bar; informative either way (a small gap means Insimul can shrink the grounding block).

| Metric | grounded − ungrounded |
| --- | ---: |
| `parseRate` | -0.500 |
| `structuralValidity` | +0.000 |
| `schemaValidity` | +1.000 |
| `referentialIntegrity` | +0.000 |
| `fullyValid` | +1.000 |
| `reachabilityCharitable` | +0.000 |
| `reachabilityStrict` | +1.000 |
| `fireabilityIndex` | +0.000 |
| `evalLoss` | +0.024 |

Run-to-run spread at a pinned seed peaks at **0.001** (`evalLoss`) — any claimed effect smaller than that is the platform's float nondeterminism, not the model.

Reproduce: `cd ml && uv pip install trl peft accelerate && uv run pinakes-train-slm --config configs/slm-pilot-3b.json` (local/GPU-only; `--stub` is the CI-safe wiring smoke). Runbook: `docs/slm-pilot-runbook.md`.

<!-- SLM-PILOT:END -->

<!-- EDIT-OPS-PILOT:START (generated by pinakes-train-edit-ops) -->

## Edit-ops SLM pilot — the 3B baseline (Phase E, edit-ops-slm-pilot US-003)

Base model `Qwen/Qwen2.5-3B-Instruct`, QLoRA on the DVC-pinned edit-ops SFT corpus, scored by the US-001 **frozen referee** on the frozen eval set (`docs/edit-ops-slm-protocol.md`) — plus the `analyzer-apply-ops` oracle tier, run through Analyzer's own `apply_ops` in its checkout. 3 repeat(s) at one pinned seed; a cell shows the mean and, where the repeats disagreed, the observed range.

- eval set `sha256 5f5ed2205b86…`, frozen: `True` — 19 cases (6 refusal, 18 with a timeline), held out run-synth-0001
- trained on 1 records (0 schema-valid targets), DVC tree `f20307afdace3832ac9a415db5f57699.dir`
- device `mps`, wall clock 1575.4 s, cost $0.00 (local Apple-Silicon MPS run — no GPU rental)
- `dryRunPass` scope: hand-written cases with a timeline, refusal cases excluded; a reply with no batch scores False; raw (uncanonicalised) ops are sent to apply_ops

> ⚠️ **`dataFloor.verdict == "insufficient-data"`.** The numbers below describe a *pipeline that completes the loop*, not an approach that validated. Per protocol §5 no verdict about the method may be drawn from them; US-006 reads this field rather than re-deriving it.

| Comparison point | `batchParseRate` | `schemaValid` | `dryRunPass` | `normalizedBatchMatch` | `refusalCorrectness` | `evalLoss` |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `analyzer-deterministic-regex-tier` | 1.000 | 1.000 | 0.083 | 0.077 | 1.000 | — |
| `untuned-qwen2.5-3b-instruct` | 0.947 | 0.947 | 1.000 | 0.538 | 0.500 | 0.64 |
| `finetuned-qwen2.5-0.5b-instruct` | 1.000 | 0.368 | 0.333 | 0.154 | 0.000 | 0.49 |
| `finetuned-qwen2.5-3b-instruct` | 0.947 | 0.947 | 1.000 | 0.538 | 0.500 | 0.31 (0.31–0.32) |
| `finetuned-qwen2.5-3b-instruct-no-vocab` | 1.000 | 0.737 | 0.667 | 0.308 | 0.667 | 0.47 (0.46–0.47) |
| `finetuned-qwen2.5-3b-instruct-no-grounding` | 1.000 | 0.895 | 0.833 | 0.308 | 0.667 | 0.37 (0.36–0.37) |
| `reference-frontier-model` | — | — | — | — | — | — |

Not measured, and why — a frozen comparison point is never dropped, only explained:

- `reference-frontier-model` — no GEMINI_API_KEY/GOOGLE_API_KEY in this environment — rerun with a key and `--frontier` to fill this row.

### Ablation — what did tuning internalise?

Fine-tuned weights on the grounded prompt **minus** the same weights on the ablated prompt. `no-vocab` removes the 12-op menu, `no-grounding` removes the Inputs/Timeline tables. Not part of the success bar (protocol §1): a small `no-vocab` gap means Analyzer can shrink the menu it sends on every request; a large `no-grounding` gap is the expected result and confirms the timeline must stay prompt-supplied.

| Metric | grounded − `no-vocab` | grounded − `no-grounding` |
| --- | ---: | ---: |
| `batchParseRate` | -0.053 | -0.053 |
| `opNameValidity` | +0.158 | -0.053 |
| `paramSchemaValidity` | +0.000 | -0.053 |
| `refGrounding` | +0.000 | +0.143 |
| `schemaValid` | +0.211 | +0.053 |
| `dryRunPass` | +0.333 | +0.167 |
| `exactBatchMatch` | +0.231 | +0.231 |
| `normalizedBatchMatch` | +0.231 | +0.231 |
| `refusalCorrectness` | -0.167 | -0.167 |
| `falseRefusalRate` | +0.000 | +0.000 |
| `evalLoss` | -0.152 | -0.052 |

### The frozen bars, as arithmetic (no verdict — that is US-006's)

| Bar | Frozen threshold | Measured |
| --- | --- | --- |
| coverage (primary) | `dryRunPass` ≥ 50% more than the regex floor, `schemaValid` ≥ 90% | floor 0.083, tuned 1.000, relative gain +1100.0%, `schemaValid` 0.947 |
| tuning-did-something | `schemaValid` ≥ +10pp vs untuned | +0.0pp (0.947 → 0.947) |
| refusal safety | `refusalCorrectness` ≥ 80%, `falseRefusalRate` ≤ +10pp | 0.500, regression +0.0pp |
| 0.5B vs 3B (cost/quality) | not a bar — US-006's model-size input | `schemaValid` 0.368 → 0.947 (+57.9pp) |

Run-to-run spread at a pinned seed peaks at **0.005** (`evalLoss`) — any claimed effect smaller than that is the platform's float nondeterminism, not the model. At 19 eval cases one case is 0.053, which is the resolution floor of every rate above.

Reproduce: `cd ml && uv pip install trl peft accelerate && uv run pinakes-train-edit-ops --config configs/edit-ops-pilot-3b.json --report --analyzer-dir ~/Development/analyzer` (local/GPU-only; `--stub` is the CI-safe wiring smoke). Runbook: `docs/edit-ops-slm-runbook.md`.

<!-- EDIT-OPS-PILOT:END -->
