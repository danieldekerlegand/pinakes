// @generated by scripts/gen-contract-bindings.ts from contracts/predicate-mapping.json — DO NOT EDIT.
// The vendored koine registry's version + bridged-project vocabulary (the JSON itself is a MIRROR — re-vendor, never hand-edit).
// Change the JSON, then run `npm run gen:contracts`. The drift gate fails a stale binding.


/** The koine `registryVersion` the vendored mirror is at. */
export const PREDICATE_REGISTRY_VERSION = "0.4.2";

/** A project the registry bridges. */
export type PredicateMappingProject =
  | "analyzer"
  | "insimul";

/** Every bridged project, in registry order. */
export const PREDICATE_MAPPING_PROJECTS = [
  "analyzer",
  "insimul",
] as const;
