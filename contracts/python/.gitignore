# Python build/runtime artifacts. Mirrors engine/.gitignore and
# services/api/.gitignore — the repo root's .gitignore covers the JS toolchain,
# not these.
__pycache__/
*.py[cod]
*.egg-info/
.eggs/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
