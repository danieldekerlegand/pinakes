# `infra/` — deployment and local-orchestration config

**Status: empty placeholder.** Part of the target repo skeleton from
[`docs/UNIFIED-PROJECT-PLAN.md` §4](../docs/UNIFIED-PROJECT-PLAN.md). Nothing has
moved here yet; `docker-compose.yml` is still at the repo root.

## Purpose

Compose files, Dockerfiles, and deploy config — pulled off the root so the root
holds README, LICENSE, the uv workspace manifest, and `.gitignore` and nothing
else structural.

## Moves in later

| Current | Note |
|---|---|
| `docker-compose.yml` | its service was renamed `culturescrape` → `pinakes_engine` with the engine (US-1) |
| Dockerfiles | |
