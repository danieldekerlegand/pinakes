# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Ralphy runtime state (regenerated each run)
.ralphy/

# Generated data — never committed. A corpus is regenerable from the category
# specs and is published as release artifacts instead (see docs/storage.md).
# That includes the `out/pinakes-full` build (formerly DVC-pinned — see
# docs/artifact-versioning.md).
/out/
.http-cache/
