# Generated by `culturescrape generate` from conflicts.yml.
# Edit the blueprint and regenerate rather than editing this file.
# Schema: docs/data-model.md ("Category specification (input)").
id: military-operations
label: Event
description: Every military operation
source:
  type: wikidata-sparql
  query: |
    SELECT ?item ?itemLabel ?image WHERE {
      ?item wdt:P31 wd:Q645883 .
      SERVICE wikibase:label { bd:serviceParam wikibase:language "en". }
    }
dimensions:
- temporal
- geographic
links:
- type: LOCATED_IN
  to: place
