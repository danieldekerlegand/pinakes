# Ralph Progress Log
Started: Thu Mar 12 02:32:13 CDT 2026
---

## 2026-03-18 - US-098
- Added 11 new guided journey narratives to `lexicons/narratives.tsv` (total: 21 narratives)
- New narratives: journey_of_coffee, viking_linguistic_trail, persian_literary_empire, dravidian_legacy, greek_scientific_legacy, creole_genesis, uralic_journey, tibetan_himalayan, printing_press_revolution, chinese_characters_across_asia, atlantic_slave_trade_languages
- Each narrative has 6 steps with map_center, map_zoom, time_point, highlighted_entities, and layer_config
- Topics span diverse language families, time periods (-7000 BCE to 2000 CE), and geographic regions
- References Phase 7-9 data layers: languages, trade_routes, migrations, civilizations, religions, archaeological_sites, haplogroups
- Created `test/test-narratives.ts` with 1395 assertions validating: TSV parsing, unique IDs, required fields, step structure (coordinates, zoom, time), content spot checks, and new + original narrative preservation
- Files changed: `lexicons/narratives.tsv`, `test/test-narratives.ts` (created)

## 2026-03-18 - US-072
- Created `lexicons/cultural-lineages.tsv` with 95 lineage relationships
- Added `CulturalLineage` interface, TSV loader, and recursive ancestor/descendant traversal to `server/tsv-storage.ts`
- Added 4 API endpoints: GET /api/cultural-lineages (with filters), /:id, /ancestors/:id, /descendants/:id
- Created `CulturalLineageExplorer` D3 force-directed graph visualization with temporal x-positioning, color-coded edges, confidence-based widths, search, and node detail cards
- Integrated as "Lineage" tab in LanguageFamilyVisualization (added ViewMode, viewSettings, tab trigger/content)
- Fixed pre-existing syntax errors in tsv-storage.ts (missing braces, interleaved methods)
- 39 test assertions passing in `test/test-cultural-lineages.ts`
- Files: cultural-lineages.tsv, tsv-storage.ts, routes.ts, CulturalLineageExplorer.tsx, LanguageFamilyVisualization.tsx, types.ts, VisualizationContext.tsx, test-cultural-lineages.ts
---

## 2026-03-26 - US-202 / US-216
- Created reusable `TreeVisualization` component at `client/src/components/visualizations/shared/TreeVisualization.tsx`
  - Generic typed tree data: accepts `TreeNodeData` with id, label, parentId, children, metadata
  - Supports horizontal, vertical, and radial tree layouts via `orientation` prop
  - Customizable node rendering: `nodeColor`, `nodeRadius`, `nodeLabel`, `nodeSubLabel` callbacks
  - Custom tooltip via `renderTooltip` render prop
  - Legend support via `legendItems` array
  - SVG/PNG export, zoom/pan, auto-fit to viewport
  - Uses existing `useVisualizationResize` hook and `createZoomBehavior` D3 helper
- Created `WritingSystemEvolutionView` at `client/src/components/visualizations/WritingSystemEvolutionView.tsx`
  - Uses shared TreeVisualization for rendering
  - Fetches writing systems from `/api/writing-systems` and builds parent-child tree
  - Nodes colored by script type (alphabet, abjad, abugida, syllabary, logographic, featural)
  - Sample glyphs displayed as sub-labels on each node
  - Detail panel showing origin date, region, unicode block, sample characters, parent/descendant navigation
  - 11 tests passing covering tree building, color mapping, date formatting, and interface validation
- Files: TreeVisualization.tsx, WritingSystemEvolutionView.tsx, WritingSystemEvolutionView.test.ts
---

## 2026-03-27 - US-308
- Created `client/src/lib/visualization/settlement-lifecycle.ts` with pure temporal lifecycle logic
  - `getLifecycleState(foundedYear, abandonedYear, currentYear)` returns phase and 0-1 visibility
  - Five lifecycle phases: hidden → founding (fade-in) → active → abandoning (fade-out) → destroyed (burst) → hidden
  - 50-year transition windows for smooth fade-in/fade-out
  - 20-year destruction burst window after abandonment
  - `capitalPulse(currentYear, reducedMotion)` returns sin-wave 0.85-1.0 multiplier for capital pulsing
  - Respects reduced-motion preference (returns constant 1.0)
- Created `client/src/components/visualizations/map-layers/SettlementsLayer.tsx`
  - Renders settlements as CircleMarkers filtered by temporal visibility
  - Settlements fade in at foundedYear and fade out at abandonedYear
  - Capitals get pulsing highlight effect via golden border + radius oscillation
  - Destroyed settlements show brief red burst effect with dashed border and 1.8x radius
  - Marker size scales with peakPopulation, color derived from civilizationId hash
  - Popup shows type, period, population, region, notable features, and lifecycle status indicators
  - Supports marker clustering for large datasets (>150 visible)
  - Follows existing ArchaeologicalSitesLayer patterns for consistency
- Created `test/test-settlement-lifecycle.test.ts` with 21 passing tests
  - Tests all lifecycle phases and transitions
  - Tests visibility linear interpolation during founding/abandoning
  - Tests capitalPulse with and without reduced motion
  - Tests edge cases: null dates, BCE/CE years, exact boundary years
  - Tests chronological phase ordering with no gaps
- Files: settlement-lifecycle.ts, SettlementsLayer.tsx, test-settlement-lifecycle.test.ts
---

## 2026-04-16 - US-420
- Created reusable `ImageGallery` component at `client/src/components/shared/ImageGallery.tsx`
  - Responsive grid and masonry layout variants
  - Lazy-loaded thumbnails with shimmer placeholders and error fallbacks
  - Lightbox built on Radix Dialog: ESC to close, arrow keys / buttons to navigate
  - Zoom (1x-5x) and click-and-drag pan in lightbox; +/-/0 keyboard shortcuts
  - Image metadata overlay: title, description, media type badge, date, license, attribution, source link, tags
  - Filterable by media type and tag; sortable by relevance, date, or title
  - Supports all media-assets media types (image, illustration, reconstruction, map, diagram, 3d_model, video)
- Extracted pure logic into `client/src/components/shared/image-gallery-utils.ts`
  - Types: `GalleryImage`, `GallerySortOrder`
  - Pure helpers: `normalizeLicense`, `getMediaTypeColor`, `getUniqueTags`, `filterByTag`, `filterByMediaType`, `sortImages`, `getAspectRatio`, `clampIndex`, `nextIndex`, `prevIndex`, `clampZoom`
- 36 passing tests in `client/src/components/shared/image-gallery-utils.test.ts` covering filters, sorting, license normalization, tag extraction, zoom clamping, and carousel wraparound
- Files: ImageGallery.tsx, image-gallery-utils.ts, image-gallery-utils.test.ts

---

## 2026-04-16 - US-430
- Built `SceneGenerator` visualization component at `client/src/components/visualizations/SceneGenerator.tsx`
  - Form controls: scene type (daily_life, ceremony, battle, marketplace, landscape, city, architecture, artifact), style (realistic/illustrated/watercolor/archaeological_sketch), time period, region, specific elements
  - Calls `POST /api/media/generate` (US-409) via react-query mutation with animated loading progress bar
  - Displays generated image as a data URL with a clearly-visible "AI-Generated Reconstruction" watermark overlay + style badge
  - Regenerate button (re-runs mutation with current form), Save to Gallery button posts to `POST /api/media-assets` and invalidates the `culture_profile` media-assets query so the cached scene appears in `CultureMediaGallery`
  - Clear error state for both generation and save failures
- Extracted pure logic into `client/src/components/visualizations/scene-generator-utils.ts`
  - UI scene types map to the four backend scene types (`city_reconstruction`, `architectural`, `daily_life`, `artifact`) so no existing tests/validation break
  - `buildSceneDescription` composes scene-type-specific phrasing + culture name + optional extra elements
  - `buildGenerateRequest` falls back to culture's time period/region when the form leaves them blank
  - `buildCacheAssetPayload` produces a ready-to-save media-asset with a data URL, generated license, watermark attribution, and scene/style/culture tags for reuse
- Added `"culture_profile"` to `VALID_ENTITY_TYPES` in `server/services/media-asset-service.ts` so generated scenes can be persisted under the culture profile (entity type was already referenced by `culture-profile-panel.tsx` and spec US-407)
- 24 new passing tests in `client/src/components/visualizations/SceneGenerator.test.ts` covering scene option coverage, UI→backend scene mapping, description composition, form validation, request building with fallbacks, data URL construction, and cache asset payload shape
- Files: SceneGenerator.tsx, scene-generator-utils.ts, SceneGenerator.test.ts, media-asset-service.ts
