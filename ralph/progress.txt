# Ralph Progress Log
Started: Thu Mar 12 02:32:13 CDT 2026

## Codebase Patterns
- TSV files live in `lexicons/` directory with tab-separated columns
- Language IDs in `languages.tsv` use ISO 639-3 codes (e.g., `eng`, `cmn`, `arb`)
- JSON arrays/objects are embedded directly in TSV fields (properly escaped with double quotes)
- Typecheck command: `npm run check` (runs `tsc`)
- Pre-existing type errors exist in the codebase; data-only TSV changes don't affect typecheck
- Always validate language_id values against languages.tsv before committing
- TSV loader pattern: private cached field → private load method (with parseTsv/getIdx) → async get methods with optional filters
- API route pattern: GET /api/{resource} (list with filters), GET /api/{resource}/:id (single), GET /api/languages/:id/{resource} (by language)
- Routes are registered in server/routes.ts, storage methods in server/tsv-storage.ts
- Interface types for TSV data are exported from tsv-storage.ts
- Client code cannot import from server — duplicate interfaces locally for API response types
- TypeScript target doesn't support Set spreading; use `Array.from(set)` instead
- UI panel pattern: sliding right panel with `isOpen`/`onClose` props, overlay backdrop, triggered from header buttons
- Some API endpoints return `{ items: [...], count: N }` objects, not flat arrays — check response shape
- D3 hierarchy node `.x`/`.y` can be `undefined` in strict TS — use `?? 0` for safe access
- URL param persistence: use `history.replaceState` (not pushState) and skip initial render with `useRef` flag
- Layer presets defined in `LAYER_PRESETS` array in `geospatial-types.ts`; empty `layers` array = show all

---

## 2026-03-12 - US-018
- Created `lexicons/phonological-inventories.tsv` with 112 languages across all major families
- Columns: id, language_id, consonants (JSON IPA array), vowels (JSON IPA array), tones (JSON array or null), phonotactic_patterns (JSON), syllable_structure, stress_system
- Coverage: Indo-European (30), Sino-Tibetan (6), Afro-Asiatic (7), Niger-Congo (15), Austronesian (8), Uralic (8), Turkic (8), Dravidian (4), Japonic (1), Koreanic (1), Austroasiatic (2), Tai-Kadai (2), Mongolic (1), Eskimo-Aleut (1), Ainu (1), Basque (1), Yeniseian (1), Historical (7+), others
- Historical languages included: lat, san, grc, got, ang, goh, peo, ave, non, chu, cop, sux, hit, akk, egy, pli
- Click consonants included for zul and xho
- Tonal data included for cmn, yue, wuu, nan, vie, tha, lao, yor, ewe, aka, lug, ibo, zul, xho, mya, bod
- Files changed: lexicons/phonological-inventories.tsv (new), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Some common language codes (guj, mar, pan, urd, sin, que) are NOT in languages.tsv - always validate against actual IDs
  - Use `awk -F'\t' 'NR>1{print $1}' lexicons/languages.tsv | sort` to get valid IDs
  - JSON in TSV fields must be on a single line with no unescaped tabs
  - The codebase has ~1099 languages in languages.tsv
---

## 2026-03-12 - US-019
- Added `PhonologicalInventory` interface to `server/tsv-storage.ts`
- Added `loadPhonologicalInventories()` private loader with JSON field parsing for consonants, vowels, tones, and phonotactic_patterns
- Added `getPhonologicalInventories(languageId?)`, `getPhonologicalInventory(id)`, `getPhonologicalInventoryByLanguage(languageId)` methods
- Added three API routes in `server/routes.ts`:
  - `GET /api/phonological-inventories` (with optional `language_id` query param)
  - `GET /api/phonological-inventories/:id`
  - `GET /api/languages/:id/phonological-inventory`
- Files changed: server/tsv-storage.ts, server/routes.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - The `tones` field needs special handling: it can be `null` (string literal) or a JSON array — parse accordingly
  - Follow the haplogroups/religions pattern for adding new domain data: interface → cached field → load method → get methods → routes
  - Pre-existing type errors (19) are unrelated to server code; server changes won't introduce new ones
---

## 2026-03-12 - US-020
- Created `PhonologyPanel` component (`client/src/components/phonology-panel.tsx`) with:
  - Language selector allowing 2-4 languages with search, color-coded badges
  - Full IPA consonant chart (place of articulation × manner of articulation grid) with color-coded phonemes per language
  - SVG vowel trapezoid chart with positioned vowel markers using language colors
  - Color-coded overlap highlighting (yellow background for shared phonemes)
  - Summary statistics table: total consonants, vowels, tones, syllable structure, stress system
- Added Music icon button to dashboard header navigation for opening the phonology panel
- Wired panel into dashboard with isOpen/onClose pattern matching existing panels
- Files changed: client/src/components/phonology-panel.tsx (new), client/src/pages/dashboard.tsx, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Cannot import server types in client code — define interfaces locally instead of importing from server/tsv-storage.ts
  - TypeScript target doesn't support Set spreading (`[...set]`), use `Array.from(set)` instead
  - The app uses right-sliding panel pattern for all comparison views, not separate routes
  - Panel pattern: `isOpen` boolean prop, overlay with `bg-black bg-opacity-50`, fixed right panel
  - Available IPA mappings: ~100+ consonants and ~30 vowels mapped to chart positions
  - Manual browser verification needed (no dev-browser skill available)
---

## 2026-03-12 - US-021
- Created `lexicons/grammar-features.tsv` with 105 languages across all major typological profiles
- Columns: id, language_id, word_order, morphological_type, case_system (JSON), gender_system (JSON), number_system (JSON), tense_aspect_mood (JSON), agreement_system, negation_strategy, question_formation, relative_clause_strategy, noun_class_count, verb_valency_changes (JSON), evidentiality, ergativity
- Morphological type coverage: fusional (49), agglutinative (41), isolating (11), polysynthetic (4)
- Word order coverage: SVO (49), SOV (38), VSO (8), V2 variants (8), free (1)
- Ergativity coverage: nominative-accusative (majority), ergative-absolutive (eus, ikt, ale, abk, ady), split-ergative (kat, hin, sux), none (isolating languages)
- Evidentiality systems included: Turkish (-mIş), Bulgarian (renarrated mood), Estonian (quotative mood)
- Historical languages: lat, san, grc, ang, got, non, peo, akk, sux, hit, egy
- Polysynthetic languages: moh (Mohawk), ain (Ainu), ikt (Inuktitut), ady (Adyghe)
- Files changed: lexicons/grammar-features.tsv (new), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - The `case_system` JSON field can range from `["none"]` for isolating languages to 15+ cases for Finnish/Estonian
  - Amharic (amh) aspects include "gerundive" — careful with JSON array syntax in TAM field
  - Some languages have complex alignment: Georgian is split-ergative, Basque is fully ergative-absolutive
  - Noun class counts vary widely: 0 for most, 10-26 for Bantu and Niger-Congo languages
  - `ekk` is Estonian (not `est`), `srb` is Serbian (not `srp`), `arb` is Standard Arabic (not `ara`) in this dataset
---

## 2026-03-12 - US-022
- Added `GrammarFeatures` interface to `server/tsv-storage.ts` with all 16 TSV columns mapped to typed fields
- Added `loadGrammarFeatures()` private loader with JSON field parsing for case_system, gender_system, number_system, tense_aspect_mood, verb_valency_changes
- Added `getGrammarFeatures(languageId?, wordOrder?, morphologicalType?)`, `getGrammarFeaturesById(id)`, `getGrammarFeaturesByLanguage(languageId)` methods
- Added three API routes in `server/routes.ts`:
  - `GET /api/grammar-features` (with optional `language_id`, `word_order`, `morphological_type` query params)
  - `GET /api/grammar-features/:id`
  - `GET /api/languages/:id/grammar-features`
- Files changed: server/tsv-storage.ts, server/routes.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Grammar features TSV has 16 columns; 5 are JSON arrays (case_system, gender_system, number_system, tense_aspect_mood, verb_valency_changes), rest are strings/numbers
  - `noun_class_count` is numeric (parseInt), defaults to 0
  - The getGrammarFeatures method supports 3 optional filters vs phonological's 1 — follow acceptance criteria for filter params
  - Pre-existing type errors (19) remain unchanged; server changes don't affect them
---

## 2026-03-12 - US-023
- Created `GrammarPanel` component (`client/src/components/grammar-panel.tsx`) with:
  - Full matrix view: languages as rows, grammatical features as columns
  - Color-coded cells for word order (SVO green, SOV blue, VSO orange), morphological type, ergativity
  - Sortable columns (click header to toggle asc/desc)
  - Feature category filter (All, Word Order, Morphology, Case & Agreement, TAM & Verb, Syntax)
  - Structural similarity score between any two selected languages (Jaccard for arrays, exact match for strings)
  - Rare feature highlighting (amber dot for features appearing in <5% of languages)
- Wired panel into dashboard with BookOpen icon button in header navigation
- Added `grammarOpen` state and `GrammarPanel` component to dashboard
- Files changed: client/src/components/grammar-panel.tsx (new), client/src/pages/dashboard.tsx
- **Learnings for future iterations:**
  - GrammarFeatures interface needs an index signature `[key: string]: string | string[] | number` to allow dynamic property access without TS errors
  - The grammar-panel uses `enabled: isOpen` on queries to avoid fetching data when panel is closed — good pattern for heavy panels
  - Manual browser verification needed (no dev-browser skill available)
---

## 2026-03-12 - US-024
- Created `lexicons/writing-systems.tsv` with 60 writing systems covering all major script families
- Columns: id, name, type, direction, parent_system_id, language_ids (JSON), origin_date, origin_region, character_count, sample_characters, unicode_block, is_active
- Script types: alphabet (13), abjad (8), abugida (18), syllabary (6), logographic (3), featural (1)
- Directions: LTR (majority), RTL (Arabic, Hebrew, Nabataean, Syriac, Avestan, Sogdian, N'Ko), TTB (Traditional Mongolian, Oracle Bone)
- Evolution chains preserved: Proto-Sinaitic -> Proto-Canaanite -> Phoenician -> Greek -> Latin; Brahmi -> all South/Southeast Asian scripts; Aramaic -> Arabic, Hebrew, Syriac, Nabataean, Sogdian -> Mongolian
- Active vs historical: 28 active, 32 historical/extinct
- All language_ids validated against languages.tsv (no invalid references)
- Files changed: lexicons/writing-systems.tsv (new), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Writing system parent chains need intermediate nodes (e.g., Phoenician-derived, Brahmi-derived) to model the full evolution tree cleanly
  - `language_ids` is a JSON array of language codes, unlike other TSV files that use a single `language_id` field
  - Some writing systems (Linear A, Brahmi, Proto-scripts) have empty language_ids `[]` — valid for undeciphered or ancestor scripts
---

## 2026-03-12 - US-025
- Added `WritingSystem` interface to `server/tsv-storage.ts` with 12 fields (id, name, type, direction, parentSystemId, languageIds, originDate, originRegion, characterCount, sampleCharacters, unicodeBlock, isActive)
- Added `loadWritingSystems()` private loader with JSON parsing for languageIds array, parseInt for characterCount, boolean parsing for isActive
- Added `getWritingSystems(type?, direction?, isActive?)`, `getWritingSystemById(id)`, `getWritingSystemDescendants(id)` methods
- Descendants method uses BFS traversal via parentSystemId to find all children recursively
- Added four API routes in `server/routes.ts`:
  - `GET /api/writing-systems` (with optional `type`, `direction`, `is_active` query params)
  - `GET /api/writing-systems/:id`
  - `GET /api/writing-systems/:id/descendants` (returns parent + all descendant systems)
- Files changed: server/tsv-storage.ts, server/routes.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Writing systems use `parentSystemId` for tree relationships — descendants endpoint uses BFS queue traversal
  - `isActive` is a boolean field parsed from "true"/"false" strings in TSV; filter param comes as string from query
  - Writing systems have `languageIds` (plural JSON array) unlike phonological/grammar which have singular `languageId`
  - The descendants endpoint returns both the parent system and all descendants for complete tree context
---

## 2026-03-12 - US-026
- Created `WritingSystemsPanel` component (`client/src/components/writing-systems-panel.tsx`) with:
  - D3.js family tree visualization showing script evolution with zoom/pan, color-coded by type
  - Grid view with filterable cards showing all writing systems
  - Detail panel showing: name, type, direction, sample characters (Unicode rendered), associated languages, origin date/region, parent/descendant navigation
  - Filter/search by type (alphabet, abjad, abugida, syllabary, logographic, featural) and active/historical status
  - Type legend with color-coded badges
  - Tree nodes labeled with script name and sample characters
- Added Type icon button to dashboard header navigation for opening the panel
- Wired panel into dashboard with `writingSystemsOpen` state
- Files changed: client/src/components/writing-systems-panel.tsx (new), client/src/pages/dashboard.tsx, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - D3 hierarchy `.y` and `.x` can be `undefined` in TS strict mode — use nullish coalescing (`?? 0`) to satisfy type checker
  - D3's `.filter()` with a string predicate causes TS errors — use `Boolean()` wrapper for truthy checks
  - Writing systems API returns `{ systems: [...], count: N }` — not a flat array like phonological-inventories
  - Family tree uses `parentSystemId` to build hierarchy; multiple root nodes get wrapped in virtual root
  - Manual browser verification needed (no dev-browser skill available)
---

## 2026-03-12 - US-027
- Created `lexicons/verb-paradigms.tsv` with 94 entries across 43 languages
- Columns: id, language_id, verb_concept, infinitive_form, conjugation_table (JSON), irregular (boolean), complexity_score, notes
- Three verb concepts covered: 'to be' (40 entries), 'to go' (25 entries), 'to give' (29 entries)
- Morphological type coverage: fusional (22 languages), agglutinative (7), isolating (5), polysynthetic (3), language isolates (2)
- Historical languages included: lat, grc, san
- Polysynthetic coverage: moh (Mohawk), ikt (Inuktitut), ain (Ainu)
- Polypersonal verbs: kat (Georgian), eus (Basque), moh (Mohawk) show multi-argument agreement
- Files changed: lexicons/verb-paradigms.tsv (new), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - conjugation_table JSON structure varies significantly by language type: fusional languages have tense/person grids, isolating languages have simple statement/negation pairs, polysynthetic languages may use agent>patient notation
  - Armenian (hye) Unicode characters need careful handling — use Python Unicode escapes to avoid encoding issues
  - complexity_score roughly correlates: polysynthetic (16-36), fusional (8-30), agglutinative (9-24), isolating (2-5)
  - 'to give' is typologically interesting — it triggers ditransitive alignment, honorific systems (Korean), and polypersonal agreement (Basque, Georgian)
---

## 2026-03-12 - US-028
- Added `VerbParadigm` interface to `server/tsv-storage.ts` with fields: id, languageId, verbConcept, infinitiveForm, conjugationTable (parsed JSON), irregular (boolean), complexityScore (number), notes
- Added `loadVerbParadigms()` private loader with JSON parsing for conjugationTable, boolean parsing for irregular, parseInt for complexityScore
- Added `getVerbParadigms(languageId?, verbConcept?)`, `getVerbParadigmById(id)`, `getVerbParadigmsByLanguage(languageId)` methods
- Note: `getVerbParadigmsByLanguage` returns an array (not single item) since a language has multiple verb paradigms (one per verb concept)
- Added three API routes in `server/routes.ts`:
  - `GET /api/verb-paradigms` (with optional `language_id`, `verb_concept` query params)
  - `GET /api/verb-paradigms/:id`
  - `GET /api/languages/:id/verb-paradigms`
- Files changed: server/tsv-storage.ts, server/routes.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Verb paradigms are 1-to-many per language (multiple verb concepts per language), unlike grammar-features which is 1-to-1 — so the `byLanguage` method returns an array, not a single item
  - `conjugationTable` JSON structure varies wildly by language type — parsed as `Record<string, unknown>` for flexibility
  - Pre-existing type errors (19) remain unchanged; server changes don't affect them
---

## 2026-03-12 - US-029
- Created `VerbParadigmsPanel` component (`client/src/components/verb-paradigms-panel.tsx`) with:
  - Verb concept selector (to be, to go, to give) to choose which verb to compare
  - Language selector allowing up to 3 languages with search, color-coded badges
  - Single language view: full conjugation table with all tenses as columns, persons as rows
  - Side-by-side comparison: summary stats (infinitive, complexity score, distinct forms, irregularity markers)
  - Per-tense comparison tables with language-colored headers showing forms for each person
  - Shared form highlighting (yellow dot) when two languages have identical forms for a tense+person
  - Shared tense/mood category indicators (e.g., Romance languages all having future tense)
  - Complexity comparison bar chart
  - Notes display for each paradigm
- Added Languages icon button to dashboard header navigation for opening the panel
- Wired panel into dashboard with `verbParadigmsOpen` state and `VerbParadigmsPanel` component
- Files changed: client/src/components/verb-paradigms-panel.tsx (new), client/src/pages/dashboard.tsx, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Verb paradigms API returns `{ paradigms: [...], count: N }` — use `.paradigms` to access array
  - conjugationTable is `Record<string, Record<string, string>>` — tenses as outer keys, person forms as inner keys
  - Person keys follow pattern: 1sg, 2sg, 3sg, 1pl, 2pl, 3pl — but some languages may have different keys
  - `Languages` icon from lucide-react works well for verb-related UI elements
  - Manual browser verification needed (no dev-browser skill available)
---

## 2026-03-12 - US-030
- Expanded `lexicons/civilizations.tsv` from 2 to 34 civilizations covering all required regions
- Expanded `lexicons/civilization-boundaries.tsv` from 2 to 80+ boundary entries with multiple temporal snapshots
- Civilizations added: Sumerian, Akkadian, Babylonian, Assyrian, Ancient Egypt, Persian (Achaemenid), Maurya, Gupta, Han Dynasty, Tang Dynasty, Mongol Empire, Ottoman Empire, Byzantine Empire, HRE, Aztec, Inca, Maya, Khmer, Mali, Songhai, Great Zimbabwe, Carthage, Phoenicia, Indus Valley, Shang Dynasty, Hittite, Umayyad Caliphate, Abbasid Caliphate, Viking territories, Celtic territories, Mughal Empire, Sasanian Empire
- Multiple temporal snapshots per expanding/contracting empire (2-4 snapshots each)
- All GeoJSON polygons validated (closed, valid coordinates, proper structure)
- All civilization_ids in boundaries match civilizations.tsv (no orphan references)
- Files changed: lexicons/civilizations.tsv (expanded), lexicons/civilization-boundaries.tsv (expanded), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - GeoJSON coordinates are [longitude, latitude] — easy to mix up with [lat, lng]
  - Simplified polygons (5-12 points) keep file sizes reasonable while preserving recognizable shapes
  - Always validate: 1) polygon closure (first coord = last coord), 2) civilization_id references, 3) JSON validity
  - Pre-existing type errors (19) unchanged — TSV data-only changes don't affect typecheck
---

## 2026-03-12 - US-031
- Expanded `lexicons/archaeological-sites.tsv` from 3 to 65 archaeological sites across all inhabited continents
- All 27+ named sites from acceptance criteria included plus 38 additional sites
- Site types: city (29), temple (11), settlement (10), ceremonial (5), cave_art (4), burial (4), fortress (2)
- Continental coverage: Europe (19), Middle East (14), Asia (15), Americas (10), Africa (7)
- Fixed existing entries to use ISO language codes (lat, grc) instead of "latin", "ancient-greek"
- All language_ids and civilization_ids validated against respective TSV files
- Files changed: lexicons/archaeological-sites.tsv (expanded), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Several languages are NOT in languages.tsv: Rapanui (rap), Ge'ez (gez), Sinhala (sin), Marathi (mar), Tajik (tgk) — use empty arrays or closest available code
  - `swa` is Swahili (not `swh`), `qwc` is Classical Quechua (not `que`) in this dataset
  - Existing archaeological-sites.tsv used non-ISO language refs ("latin" instead of "lat") — fixed to be consistent
  - Pre-existing type errors down to 5 (from 19 noted previously) — still unrelated to data changes
---

## 2026-03-12 - US-032
- Created `lexicons/migration-routes.tsv` with 34 migration/trade/conquest routes
- Columns: id, name, route_type, waypoints (GeoJSON LineString), start_date, end_date, peoples (JSON), associated_languages (JSON), description, consequences
- Route types: migration (11), trade (8), conquest (4), colonization (2), diaspora (4), plus mixed
- All 17+ named routes from acceptance criteria included: Out of Africa, Indo-European expansion, Bantu expansion, Austronesian expansion, Polynesian voyaging, Silk Road, Trans-Saharan trade, Amber Road, Incense Route, Viking expansion, Arab expansion, Mongol conquests, European colonization routes, Atlantic slave trade, Great Migration (US), Jewish diaspora, Romani migration
- Additional routes: Beringia land bridge, Indo-Aryan migration, Turkic migrations, Crusades, Portuguese maritime, Spice trade, Trail of Tears, Phoenician trade, Han Dynasty expansion, Irish diaspora, Malay Archipelago trade, Gothic migrations, Hungarian migration, Viking Eastern route, Arab Indian Ocean trade
- All language_ids validated against languages.tsv (Mongolian uses `khk` not `mon`)
- Files changed: lexicons/migration-routes.tsv (new), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Mongolian in this dataset uses `khk` (Khalkha Mongolian), NOT `mon` — always check specific variant codes
  - GeoJSON LineString coordinates are `[longitude, latitude]` arrays — same as Polygon but without closing
  - Route types from acceptance criteria: migration, trade, conquest, colonization, diaspora
  - Some routes have empty `associated_languages` (e.g., prehistoric migrations) — valid for pre-language-family events
  - Pre-existing type errors (5) remain unchanged; data-only TSV changes don't affect typecheck
---

## 2026-03-12 - US-034
- Created `lexicons/battles.tsv` with 53 historically significant battles spanning from -1274 BCE to 1954 CE
- Columns: id, name, date, coordinates (lat/lng JSON), belligerents (JSON array of {name, civilization_id}), outcome, casualties_estimate, significance, associated_language_changes, war_name
- Continental coverage: Europe (25+), Middle East/North Africa (10+), Asia (8), Americas (5), Africa (5)
- Key language-shift battles included: Hastings (Norman French→English), Constantinople 1453 (Greek→Turkish), Tenochtitlan (Nahuatl→Spanish), Alesia (Celtic→Latin), Yarmouk (Greek/Aramaic→Arabic), Talas (Chinese→Arabic/Turkic influence)
- All civilization_id references validated against civilizations.tsv (null used for non-cataloged civilizations)
- All coordinate JSON and belligerent JSON validated
- Files changed: lexicons/battles.tsv (new), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Battles use `coordinates` as `[lat, lng]` (NOT GeoJSON `[lng, lat]` order) — different from civilization-boundaries and migration-routes
  - `belligerents` uses `civilization_id: null` for civilizations not in civilizations.tsv (e.g., Germanic tribes, Crusader states)
  - `date` is a simple year string (negative for BCE) — simpler than migration-routes date format
  - Pre-existing type errors unchanged; data-only TSV changes don't affect typecheck
---

## 2026-03-12 - US-033
- Added `MigrationRoute` interface to `server/tsv-storage.ts` with fields: id, name, routeType, waypoints (parsed GeoJSON), startDate, endDate, peoples (JSON array), associatedLanguages (JSON array), description, consequences
- Added `loadMigrationRoutes()` private loader with JSON parsing for waypoints, peoples, and associatedLanguages fields
- Added `getMigrationRoutes(routeType?, startDate?, endDate?)` and `getMigrationRouteById(id)` methods
- Added two API routes in `server/routes.ts`:
  - `GET /api/migration-routes` (with optional `route_type`, `start_date`, `end_date` query params)
  - `GET /api/migration-routes/:id`
- Files changed: server/tsv-storage.ts, server/routes.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Migration routes use GeoJSON LineString for waypoints (unlike civilizations which use Polygon)
  - `startDate` and `endDate` are strings (can be negative for BCE dates like "-70000") — string comparison works for filtering
  - Migration routes API returns `{ routes: [...], count: N }` — consistent with other collection endpoints
  - Pre-existing type errors (19 originally, now showing various client-side issues) remain unchanged; server changes don't affect them
---

## 2026-03-12 - US-035
- Added `Battle` interface to `server/tsv-storage.ts` with fields: id, name, date, coordinates ([lat, lng] tuple), belligerents (array of {name, civilization_id}), outcome, casualtiesEstimate, significance, associatedLanguageChanges, warName
- Added `loadBattles()` private loader with JSON parsing for coordinates and belligerents fields
- Added `getBattles(warName?, startDate?, endDate?, civilizationId?)` and `getBattleById(id)` methods
- Date filtering uses parseInt for numeric comparison (handles negative BCE dates correctly)
- Civilization filter checks if any belligerent's civilization_id matches
- Added two API routes in `server/routes.ts`:
  - `GET /api/battles` (with optional `war_name`, `start_date`, `end_date`, `civilization_id` query params)
  - `GET /api/battles/:id`
- Files changed: server/tsv-storage.ts, server/routes.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Battles use `coordinates` as `[lat, lng]` (NOT GeoJSON `[lng, lat]`) — parsed as tuple type `[number, number]`
  - `belligerents` is a JSON array of objects with `civilization_id` that can be `null` — filter uses `.some()` on the array
  - Date filtering for battles needs parseInt since dates are string years (can be negative for BCE)
  - Battles API returns `{ battles: [...], count: N }` — consistent with other collection endpoints
  - Pre-existing type errors (19) remain unchanged; server changes don't affect them
---

## 2026-03-12 - US-036
- Enhanced `TimeSlider` with:
  - Speed controls: 1x, 5x, 10x, 50x (years per second)
  - Step sizes: Decade (10yr), 25yr, Half century (50yr), Century (100yr)
  - Expanded era labels: Prehistoric, Bronze Age, Iron Age, Classical, Medieval, Early Modern, Modern
- Created `BattlesLayer` component (`client/src/components/visualizations/map-layers/BattlesLayer.tsx`):
  - Flash animation when timeline crosses a battle's date (yellow pulse, 2s duration)
  - Battles visible within 50-year window of current time with fade-out opacity
  - Red circle markers with popups showing name, date, belligerents, outcome, significance, language impact
- Created `TimelineEventsSidebar` component (`client/src/components/visualizations/map-layers/TimelineEventsSidebar.tsx`):
  - Shows key events within 100-year window of current time
  - Displays civilizations rising/falling, battles, archaeological sites being founded, routes beginning
  - Events sorted by proximity to current year, max 8 shown
  - Current year and era label header
- Added animated flowing routes: CSS dash-flow animation on route polylines when timeline is playing
- Added `battles` layer type to `geospatial-types.ts` with DEFAULT_LAYER_CONFIGS entry
- Integrated all new components into `EnhancedLanguageMapView`:
  - Battles data fetching from `/api/battles`
  - BattlesLayer rendering inside MapContainer
  - TimelineEventsSidebar positioned next to LayerPanel
  - Route animation prop passed based on isPlaying state
- Files changed: client/src/components/visualizations/map-layers/TimeSlider.tsx, client/src/components/visualizations/map-layers/BattlesLayer.tsx (new), client/src/components/visualizations/map-layers/TimelineEventsSidebar.tsx (new), client/src/components/visualizations/map-layers/RoutesLayer.tsx, client/src/components/visualizations/EnhancedLanguageMapView.tsx, client/src/lib/visualization/geospatial-types.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Battles API returns `{ battles: [...], count: N }` — consistent with other collection endpoints
  - Battles use `[lat, lng]` coordinates (NOT GeoJSON `[lng, lat]`), so no coordinate swapping needed for Leaflet
  - CSS animation on Leaflet polylines works via `className` prop on pathOptions — `stroke-dashoffset` animates the dash pattern
  - The `useRef` pattern for injecting `<style>` elements ensures cleanup on unmount
  - Pre-existing type errors (19) remain unchanged; client changes don't introduce new ones
  - Manual browser verification needed (no dev-browser skill available)
---

## 2026-03-12 - US-037
- Enhanced `LayerPanel` with preset layer combinations: Linguistic Atlas, Political History, Cultural Diffusion, Trade & Economy, All Layers
- Added `LAYER_PRESETS` array and `LayerPreset` interface to `geospatial-types.ts`
- Added `haplogroups` layer type and `genetics` category to `DEFAULT_LAYER_CONFIGS`
- Created `HaplogroupLayer` component (`client/src/components/visualizations/map-layers/HaplogroupLayer.tsx`) with:
  - CircleMarker rendering colored by haplogroup type (Y-DNA green, mtDNA purple)
  - Geographic coordinate resolution from region names
  - Popup with haplogroup details, associated language families, origin info
- Enhanced `useMapLayers` hook with:
  - URL parameter persistence (`layers` and `opacities` params) via `history.replaceState`
  - `applyPreset(presetId)` method and `activePresetId` detection
  - Initial state hydration from URL params on mount
- Enhanced `MapLegend` with dynamic entries for all layer types: battles, cuisines, music, religions, haplogroups, material-culture, routes (conquest)
- Added category colors for cuisines (orange), music (fuchsia), religions (indigo), genetics (emerald) in LayerPanel
- Integrated haplogroup data fetching and rendering in `EnhancedLanguageMapView`
- Files changed: client/src/lib/visualization/geospatial-types.ts, client/src/components/visualizations/hooks/useMapLayers.ts, client/src/components/visualizations/map-layers/LayerPanel.tsx, client/src/components/visualizations/map-layers/MapLegend.tsx, client/src/components/visualizations/map-layers/HaplogroupLayer.tsx (new), client/src/components/visualizations/EnhancedLanguageMapView.tsx
- **Learnings for future iterations:**
  - URL param sync uses `history.replaceState` (not pushState) to avoid polluting browser history on every toggle
  - Skip initial render URL sync with a ref flag to avoid overwriting URL params that were just read
  - Haplogroup API returns `{ haplogroups: [...], count: N }` — consistent with other collection endpoints
  - Haplogroups don't have explicit coordinates; use `geographicOrigin` string to resolve approximate coords
  - `LAYER_PRESETS` with empty `layers` array means "show all" — special case in `applyPreset`
  - Pre-existing type errors (19) remain unchanged; UI changes don't introduce new ones
  - Manual browser verification needed (no dev-browser skill available)
---
## 2026-03-12 - US-038
- Created `lexicons/language-contacts.tsv` with 95 language contact events across all major contact scenarios worldwide
- Columns: id, source_language_id, target_language_id, contact_type (substrate/superstrate/adstrate), time_period, region, features_transferred (JSON with phonological/lexical/grammatical arrays), example_features, intensity (heavy/moderate/light)
- Contact types: superstrate (38), adstrate (35), substrate (12), borrowing/mixed (10)
- Integrated and enriched data from existing `data/language_contact_phenomena.csv` (117 entries mapped to structured TSV format)
- Geographic coverage: Europe (35+), Asia (20+), Middle East (10+), Africa (10+), Americas (8), Pacific (3)
- Historical depth: from 3000BCE (Sumerian-Akkadian) to present-day English global influence
- Key contact scenarios: Norman French→English, Arabic→Spanish, Sanskrit→Southeast Asian, Chinese→Japanese/Korean/Vietnamese, Norse→English, Latin→Romance, Balkan Sprachbund, colonial contacts
- All language IDs validated against languages.tsv (no invalid references)
- Files changed: lexicons/language-contacts.tsv (new), ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Many languages from the CSV source are NOT in languages.tsv (afr, fas, gla, kur, mar, mkd, nep, pan, pus, que, rom, sin, srp, tgk, yid) — use only validated IDs
  - Contact types in this dataset: substrate, superstrate, adstrate (no pidginization/creolization entries as those require pidgin/creole language IDs which aren't in languages.tsv)
  - `features_transferred` JSON has three arrays: phonological, lexical, grammatical — some may be empty `[]`
  - The existing `data/language_contact_phenomena.csv` has rich narrative data but uses language names not IDs — manual mapping required
  - Pre-existing type errors (19) remain unchanged; data-only TSV changes don't affect typecheck
---

## 2026-03-12 - US-039
- Added `LanguageContact` interface to `server/tsv-storage.ts` with fields: id, sourceLanguageId, targetLanguageId, contactType, timePeriod, region, featuresTransferred (parsed JSON with phonological/lexical/grammatical arrays), exampleFeatures, intensity
- Added `loadLanguageContacts()` private loader with JSON parsing for featuresTransferred field
- Added `getLanguageContacts(sourceLanguageId?, targetLanguageId?, contactType?, intensity?)`, `getLanguageContactById(id)`, `getLanguageContactsByLanguage(languageId)` methods
- `getLanguageContactsByLanguage` filters on both sourceLanguageId and targetLanguageId (a language can be either source or target of a contact event)
- Added three API routes in `server/routes.ts`:
  - `GET /api/language-contacts` (with optional `source_language_id`, `target_language_id`, `contact_type`, `intensity` query params)
  - `GET /api/language-contacts/:id`
  - `GET /api/languages/:id/contacts`
- Files changed: server/tsv-storage.ts, server/routes.ts, ralph/prd.json, ralph/progress.txt
- **Learnings for future iterations:**
  - Language contacts are bidirectional — `getLanguageContactsByLanguage` checks BOTH source and target language IDs using OR filter
  - `featuresTransferred` JSON has a fixed structure: `{ phonological: [], lexical: [], grammatical: [] }` — use this as default fallback
  - Language contacts API returns `{ contacts: [...], count: N }` — consistent with other collection endpoints
  - Pre-existing type errors (19) remain unchanged; server changes don't affect them
---
