# Generated by `pinakes_engine generate` from architecture.yml.
# Edit the blueprint and regenerate rather than editing this file.
# Schema: docs/data-model.md ("Category specification (input)").
id: archaeological-sites
label: Building;CulturalArtifact
description: Every archaeological site
source:
  type: wikidata-sparql
  query: |
    SELECT ?item ?itemLabel ?image WHERE {
      ?item wdt:P31 wd:Q839954 .
      SERVICE wikibase:label { bd:serviceParam wikibase:language "en". }
    }
dimensions:
- temporal
- geographic
links:
- type: LOCATED_IN
  to: place
