# Generated by `pinakes_engine generate` from visual-art.yml.
# Edit the blueprint and regenerate rather than editing this file.
# Schema: docs/data-model.md ("Category specification (input)").
id: frescoes
label: Artwork;CulturalArtifact
description: Every fresco
source:
  type: wikidata-sparql
  query: |
    SELECT ?item ?itemLabel ?image WHERE {
      ?item wdt:P31 wd:Q22669139 .
      SERVICE wikibase:label { bd:serviceParam wikibase:language "en". }
    }
dimensions:
- temporal
- geographic
links:
- type: CREATED_BY
  to: person
- type: ORIGINATES_FROM
  to: place
