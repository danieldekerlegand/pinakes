# `tooling/` — developer scripts and repo automation

**Status: empty placeholder.** Part of the target repo skeleton from
[`docs/UNIFIED-PROJECT-PLAN.md` §4](../docs/UNIFIED-PROJECT-PLAN.md). Nothing has
moved here yet; the scripts still live in [`scripts/`](../scripts/).

## Purpose

The scripts that are genuinely *tooling* — QA gates, exporters, mirror
regenerators, secret scanning — rather than product code.

## Moves in later

| Current | Note |
|---|---|
| `scripts/` (TS) | **heavily culled**; the acquisition scripts fold into [`engine/`](../engine/) instead of moving here |
