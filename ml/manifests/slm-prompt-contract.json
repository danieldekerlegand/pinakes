{
  "chatTemplate": {
    "format": "chatml",
    "generationPrefix": "<|im_start|>assistant\n",
    "imEnd": "<|im_end|>",
    "imStart": "<|im_start|>",
    "turn": "<|im_start|>{role}\n{content}<|im_end|>\n"
  },
  "contractVersion": "1",
  "generation": {
    "maxNewTokens": 96,
    "stop": [
      "<|im_end|>",
      "<|endoftext|>"
    ],
    "temperature": 0.0,
    "topK": 1,
    "topP": 1.0
  },
  "inferencePromptTemplate": "<|im_start|>system\nYou are Insimul's rule author. Answer with exactly one Prolog rule: a single clause terminated by a period. Output only the rule — no prose, no explanation, no code fences.<|im_end|>\n<|im_start|>user\n{{USER_PROMPT}}<|im_end|>\n<|im_start|>assistant\n",
  "pipelineVersion": "1",
  "protocolVersion": "1",
  "response": {
    "extract": "Strip code fences, drop everything before the first clause-shaped line (^[a-z]\\w*\\s*\\(), and take the clause up to its terminating period — pinakes_ml.slm_finetune.extract_rule.",
    "gate": "Score every generation through the tier-4 validator (pinakes-eval-rule-adherence) before writing it into a world.",
    "parseFailure": "A generation with no clause-shaped line is a PARSE FAILURE and must be rejected, not coerced: the tier-4 parser would accept \"I cannot help.\" as a one-atom clause."
  },
  "runtime": {
    "note": "The model is a Qwen2.5-Instruct derivative, so node-llama-cpp's ChatML wrapper is correct — but send the strings below verbatim rather than relying on wrapper defaults: the system prompt and the stop set are part of what was measured.",
    "quantization": "Q4_K_M",
    "target": "Insimul LocalAIService (node-llama-cpp)"
  },
  "systemPrompt": "You are Insimul's rule author. Answer with exactly one Prolog rule: a single clause terminated by a period. Output only the rule — no prose, no explanation, no code fences.",
  "trainingTextTemplate": "<|im_start|>system\nYou are Insimul's rule author. Answer with exactly one Prolog rule: a single clause terminated by a period. Output only the rule — no prose, no explanation, no code fences.<|im_end|>\n<|im_start|>user\n{{USER_PROMPT}}<|im_end|>\n<|im_start|>assistant\n{{RULE}}<|im_end|>\n",
  "userPrompt": {
    "arms": [
      "grounded",
      "ungrounded"
    ],
    "groundedLinePrefixes": [
      "Intrinsic predicates (true at world creation):",
      "Action-producible predicates (some action effect can make these true):",
      "Entities you may name:"
    ],
    "note": "The system turn carries NO world vocabulary by design — the grounding block lives in the user turn, which is exactly what the US-003 ablation strips. Insimul builds the user turn with insimul_datasets.build_rule_prompt; send the grounded arm.",
    "production": "grounded",
    "ungroundedConstraint": "Use Insimul's standard Prolog rule vocabulary; "
  }
}
