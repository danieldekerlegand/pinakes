node_modules
dist
.DS_Store
# Secrets — never commit (use .env.example as the template). See docs note on rotation.
.env
.env.local
.env.*.local
server/public
vite.config.ts.*
*.tar.gz
sources/
.ralphy
.ralphy-worktrees
# Stranded DVC local remote — DVC has been removed (docs/artifact-versioning.md).
# Kept ignored so a leftover checkout does not show up as untracked noise.
dvc-storage/
# US-007 collaborative collections — user runtime data (JSON per collection)
data/runtime/collections/
# US-008 user annotations — user runtime data (JSON per annotation)
data/runtime/annotations/
# US-012 culture stewardship — user runtime data (stewards.json)
data/runtime/stewardship/
# US-010 data changelog — runtime audit log (JSON per change entry)
data/runtime/changelog/
# US-011 living dataset — runtime state (ingestion timestamps + release history)
data/runtime/living-dataset/
# US-... contributions — runtime queue (samples archived under data/archive/)
data/runtime/contributions/
# US-3 target-layout skeleton — build/ holds regenerable outputs only (docs/UNIFIED-PROJECT-PLAN.md §4).
# Ignore the contents but keep the placeholder README so the directory stays visible.
/build/*
!/build/README.md
# Playwright e2e (US-006) — installed browsers cache is global; test artifacts are local
test-results/
playwright-report/
blob-report/
playwright/.cache/
.chief/state/
